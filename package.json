{
  "name": "apprentorbay",
  "private": true,
  "version": "0.1.0",
  "description": "ApprentorBay — a mentorship and apprenticeship app for learners and mentors.",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "emulators": "firebase emulators:start --only auth,firestore,storage --project apprentorbay-demo",
    "dev": "concurrently -k -n emulators,client,server -c green,cyan,magenta \"npm run emulators\" \"npm run dev -w client\" \"wait-on tcp:127.0.0.1:8080 tcp:127.0.0.1:9099 && npm run dev -w server\"",
    "dev:client": "npm run dev -w client",
    "dev:server": "npm run dev -w server",
    "build": "npm run build -w shared && npm run build -w client && npm run build -w server",
    "start": "npm run start -w server",
    "typecheck": "npm run typecheck -w shared && npm run typecheck -w client && npm run typecheck -w server",
    "test": "npm run test -w shared"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "typescript": "^5.9.2"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "firebase-tools": "^14.16.0",
    "wait-on": "^8.0.4"
  }
}
